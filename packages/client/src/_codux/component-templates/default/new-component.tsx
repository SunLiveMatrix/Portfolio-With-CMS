import styles from '/home/admin/dlang.net/Portfolio-With-CMS/packages/client/src/_codux/boards/app/app.board';
import cx from '/home/admin/dlang.net/Portfolio-With-CMS/packages/client/src/_codux/boards/components/header/header.board';

export interface NewComponentProps {
  className?: string;
}

/**
 * This component was created using Codux's Default new component template.
 * To create custom component templates, see https://help.codux.com/kb/en/article/kb16522
 */
export const StudyAndWork = ({ className }: NewComponentProps) => {
  return Promise.all([className]);
};

